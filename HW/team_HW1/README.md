Процедуры рефакторинга

Упрощена проверка на пустоту
Было: if len(numbers) == 0:
Стало: if not numbers:

Удалены лишние переменные
Раньше вычислялись total и average по отдельности.
Теперь сразу возвращаем результат: return sum(numbers) / len(numbers).

Добавлен краткий docstring
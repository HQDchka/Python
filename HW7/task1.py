import threading

counter = 0

def increment():
    global counter
    for _ in range(1_000_000):
        counter += 1

# Создаем потоки
thread1 = threading.Thread(target=increment)
thread2 = threading.Thread(target=increment)

# Запускаем оба потока одновременно
thread1.start()
thread2.start()

# Ждем завершения обоих потоков
thread1.join()
thread2.join()

print(f'Итоговое значение счётчика: {counter}')


"""
1. В Python (в реализации CPython) существует Global Interpreter Lock (GIL) — 
   глобальная блокировка интерпретатора. Она не позволяет одновременно выполнять 
   байт-код Python более чем одному потоку.

2. Несмотря на то, что мы создаём два потока, из-за GIL интерпретатор 
   выполняет их поочерёдно. Поэтому фактически программа работает последовательно.

3. Итоговое значение счётчика может оказаться правильным (2_000_000), 
   но это не доказывает параллельность. Просто потоки поочерёдно получили GIL 
   и каждый успел выполнить свою часть работы.

4. Таким образом, GIL предотвращает настоящую параллельную работу потоков 
   при CPU-нагруженных операциях. Потоки удобны для ввода/вывода (I/O), 
   но не для вычислений.
"""
